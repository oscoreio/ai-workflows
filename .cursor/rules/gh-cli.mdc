---
description: 
globs: 
alwaysApply: true
---
---
description: Always include GitHub CLI manual
alwaysApply: true
---

- Use the [GitHub CLI manual](mdc:https:/cli.github.com/manual) for scripting tasks like `gh pr`, `gh issue`, `gh api`.
- Use `gh api` for authenticated downloads (e.g., images in issue comments).
- Ensure flags and auth are consistent with the CLI reference.